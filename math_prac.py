import math # You have to start with importing the math library for advanced math functions

# Absolute value function 
x = -5
abs_x = abs(x)

# Square rooot function
y = 16
sqrt_y = math.sqrt(y)

# Power function 
a = 2
b = 3
pow_ab = math.pow(a,b) 

# Exponential function
z = 1
exp_z = math.exp(z)

# Natural logarithm 
log_y = math.log(y)

# Angle in radians
angle = math.pi / 4

# Trigonometric functions
cos_val = math.cos(angle)
acos_val = math.acos(cos_val)

sin_val = math.sin(angle)
asin_val = mat.asin(sin_val)

tan_val = math.tan(angle)
atan_val = mat.atan(tan_val)

# Ceil and floor
ceil_val = math.ceil(2.3) # Ceil round up to to the nearest integer
floor_val = math.floor(2.7) # floor round down to the nearest integer

# Minimum and maximum 
min_val = min(10, 20) 
max_value = max(10, 20)